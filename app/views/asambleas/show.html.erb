
<%= render_top :asambleas %>

<div class="row24">
  <div class="col-left">
    <%= render_browser :asambleas, @project.asambleas %>
  </div>
  <div class="col-center">
    <h2>Reunión <%= @asamblea.title %></h2>
    <div class="actions">
      <%= link_to 'Crear tema', [:new, @project, @asamblea, :topic] %>
    </div>
  </div>
</div>