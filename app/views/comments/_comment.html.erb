<%= div_for comment, :class => 'model' do %>
    <p><b>Comentario</b> de <b><%= link_to comment.user.name, comment.user %></b> hace <%= time_ago_in_words comment.created_at %>:</p>
    <%= render_body comment %>
<% end %>