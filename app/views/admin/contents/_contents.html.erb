<div class="actions">
  <%= button_to 'Subir material', [:new, :admin, @project, :content] %>
</div>

<table>
  <thead>
    <tr>
      <td>Posición</td>
      <td></td>
      <td>Título</td>
      <td>Autorxs</td>
      <td>Descripción</td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% contents.each do |content| %>
      <tr class="<%= cycle('even', 'odd') %>">
        <%= position_actions(content) %>
        <td><%= content.position %></td>
        <td class="title"><%= link_to content.title, [:admin, @project, content] %></td>
        <td><%= content.authors %></td>
        <td><%= content.description %></td>
        <td class="actions">
          <%= button_to 'Editar', [:edit, :admin, @project, content], :method => :get %>
          <%= button_to 'Borrar', [:admin, @project, content], :method => :delete,
          :confirm => "Quieres borrar el proyecto '#{content.title}'!! No se puede volver atrás!!. ¿Segura?" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
